[pytest]
# Suppress harmless SQLAlchemy GC warnings from aiosqlite
filterwarnings =
    ignore::sqlalchemy.exc.SAWarning
    ignore:The garbage collector is trying to clean up:ResourceWarning
asyncio_mode = auto
